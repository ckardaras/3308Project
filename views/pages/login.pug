include ../partials/header.pug
#outer.container-fluid
  .row
    .col-sm
      #inner2
        header
          // <h1>Umbrella</h1>
          img(src='../img/UmbrellaLogo1.png' alt='Logo' height='70' width='200')
        .jumbotron
          p
            | Umbrella encompasses reliability and user friendliness in a platform that seemingly combines
            | a database, a front end, and nodejs.
            | We captured the need players and coaches have and gave them the power to record matches
            | and organize their teams.
            | Each league will be able to see a bracket that will show when and where each teams meet.
    // second column
    .col-sm
      #inner
        header
          h1 Sign In
        .jumbotron
          form(action='./auth', method='post' )
            .form-group
              label(for='username') Username
              input.form-control(type='text' placeholder='Enter Username' name='username')
              if(got_in==false)
                p(style='color:red')="Oops! Password/Username is incorrect!"
              if(user_req==false)
                p(style='color:red')="Username is required!"
            .form-group
              label(for='password') Password
              input.form-control(type='password' placeholder='Enter Password' name='password')
              if(pass_req==false)
                p(style='color:red')="Password is required!"
            .form-group
              button.btn.btn-primary(type='submit') Login
              br
              br
          .row
            .col-sm
              p="New here?"
            .col-sm
              form(action="/register")
                input.btn.btn-secondary.btn-light(type="submit" value="Sign Up")
  // Will add javascript to go through an array of testimonial
  h2 Testimonials
  .container2
    img(src='../img/chirsfox.jpg' alt='Avatar' style='width:90px')
    p
      span John Doe.
      |  CEO at Best Company.
    p "Umbrella is such an easy tool to use. It has helped us stay organize."
